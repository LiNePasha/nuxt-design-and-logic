// playwright.config.ts

const config = {
  // Include TypeScript files for test discovery
  // Adjust the path and pattern to match your test files
  testMatch: '**/*.test.ts',
};

export default config;
